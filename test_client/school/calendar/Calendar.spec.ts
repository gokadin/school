import {it, describe, expect, inject, fakeAsync,
    afterEach, beforeEachProviders, tick} from 'angular2/testing';

import {MockAvailabilityService} from "../../mocks/services/MockAvailabilityService";

describe('hello', () => {
    it('should run', () => {
        expect(true).toBe(true);
    });
});